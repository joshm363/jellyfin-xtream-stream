# SaveStrm

Web app for searching Dispatcharr content and writing `.strm` files locally on the server.

## Local development

```bash
npm install
npm run dev
```

## Docker

```bash
docker compose up --build
```

The container listens on port `3001` and stores config in `/app/data/config.json`.
